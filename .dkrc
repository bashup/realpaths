#!/usr/bin/env bash

import: dk.cram                  # run tests using the "cram" functional test tool
import: dk.entr-watch            # watch files and re-run tests or other commands
import: dk.shell-console
